# Notas:

Este es mi primer programa en Node

```
Options:
      --help     Show help                                                                 [boolean]
      --version  Show version number                                                       [boolean]
  -b, --base     Es la base de la tabla de multiplicar                         [number] [default: 5]
  -l, --listar   Muestra la tabla en consola                              [boolean] [default: false]
  -h, --hasta    Indica el limite de veces que se multiplicara la base        [number] [default: 10]
```
